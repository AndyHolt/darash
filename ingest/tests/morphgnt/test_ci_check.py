def test_intentional_failure():
    # This test is intentionally failing to verify the CI workflow catches errors.
    # Delete this file once CI is confirmed working.
    assert False, "Intentional failure — delete this file once CI is verified"
